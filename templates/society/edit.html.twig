{% extends 'base.html.twig' %}

{% block title %}Modifier la société - CRM3000{% endblock %}

{% block stylesheets %}
    {{ encore_entry_link_tags('societyModify') }}
{% endblock %}

{% block body %}
    <div class="container-society-modify">
        <div class="container-header">
            <h1>Modifier la société {{ society.name }}</h1>
        </div>
        <div class="container-body">
            {{ include('society/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
            {{ include('society/_delete_form.html.twig') }}
            <a class="regular-button blue-button" href="{{ path('society_index') }}">Retour à la liste</a>
        </div>
    </div>
    {{ encore_entry_script_tags('societyModify') }}
{% endblock %}
