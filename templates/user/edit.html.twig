{% extends 'base.html.twig' %}

{% block title %}Modification utilisateur{% endblock %}

{% block body %}
    <h1>Modifier l'utilisateur {{ user.email }}</h1>

    <a href="{{ path('user_update_password', {id: user.id}) }}">Changer le mot de passe</a>
    <a href="{{ path('user_update_email', {id: user.id}) }}">Changer l'email</a>
    <a href="{{ path('user_update_society', {id: user.id}) }}">Changer la société</a>

    <a href="{{ app.request.headers.get('referer') }}">Retour</a>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
